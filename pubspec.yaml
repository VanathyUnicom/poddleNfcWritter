name: poddle_nfc_writer
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  cupertino_icons: ^1.0.2
  poddle_nfc:
    git: https://github.com/thuvaragan-g/poddle_nfc

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

 
